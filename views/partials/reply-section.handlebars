<div >
    <button data-id="{{id}}">Add Comment</button>

    <div data-id="{{id}}">

        <div>
            <textarea data-id="{{id}}" placeholder="Write your comment here."></textarea>
        </div>
        <div class="mt-2">
            <button data-id="{{id}}">Send</button>
            <button data-id="{{id}}">Cancel</button>
        </div>

    </div>
</div>